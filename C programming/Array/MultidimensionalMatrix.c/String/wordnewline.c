#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int main(){
    char s[]="this is an";
    int n =strlen(s);
    printf("%s",s);
    char b[100];
    int i=0;
    while(i<n){
        if(s[i]==" "){
            b=arr[i];
            continue;
        }
        printf("%s",s);
        printf("\n");

        i++;
    }
}