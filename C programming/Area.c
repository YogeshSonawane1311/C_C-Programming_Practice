#include<stdio.h>
#include<math.h>
int circle(int r){
    int c = math.pi * pow(r,2);
    return c;
}
int rectangle(int l,int b){
    return l*b;
}
int square(int side){
    return pow(side,2);
}
int main(){
    
}